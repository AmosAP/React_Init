import './style.css'

// Função principal do componente Navbar
export default function Navbar() {
  // A função retorna o JSX que define a estrutura do componente
  return (
    // 1. Adicionar um elemento <nav> que atuará como o contêiner principal da barra de navegação.

    // 2. Dentro do <nav>, criar um logo ou título da aplicação. Isso pode ser feito com uma tag <img> para uma logo ou <h1>/<h2> para um texto.

    // 3. Adicionar um menu de navegação utilizando uma lista não ordenada <ul> com itens de lista <li>.
    //    Cada <li> pode conter um link <a> que direciona para diferentes partes do site.

    // 4. Opcionalmente, incluir elementos adicionais como um botão de login/logout, um ícone de busca, ou um menu de usuário.

    // 5. Adicionar classes CSS ou estilizar diretamente os elementos para definir o layout e a aparência da navbar.

    // 6. Garantir que a navbar seja responsiva, usando técnicas de CSS como flexbox ou grid, e media queries para ajustar o layout em diferentes tamanhos de tela.

    // 7. Se necessário, incluir um menu de hambúrguer para navegação em dispositivos móveis, que expande para mostrar os links ao ser clicado.
  );
}
